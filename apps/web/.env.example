# ==========================================
# Puente Fintech DApp — Environment Variables
# ==========================================
# Copy this file to .env.local and fill in the values.
# ⚠️ NEVER commit .env.local to git.

# ─── Supabase (https://supabase.com/dashboard) ───
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ─── Privy (https://dashboard.privy.io) ───
NEXT_PUBLIC_PRIVY_APP_ID=your-privy-app-id
PRIVY_APP_SECRET=your-privy-secret

# ─── Supabase Admin (⚠️ server-side ONLY — bypasses RLS) ───
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ─── Service Wallet (⚠️ CRITICAL — controls real funds) ───
# Use a dedicated hot wallet with minimal funds.
# Generate: cast wallet new
SERVICE_WALLET_PRIVATE_KEY=0x...
SERVICE_WALLET_ADDRESS=0x...
